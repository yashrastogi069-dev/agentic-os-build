# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables and local key files (never commit secrets)
.env*.local
KeysJarvis.env
*.keys.env

# Common ignores
node_modules
.next/
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo

# Agentic OS local data (SQLite DB, voice binaries and models)
data/
bin/
models/

# Phase 6 voice sidecar local env
tools/stt-server/.venv/
tools/**/__pycache__/

# Phase 7 dictation companion local env (real config holds no secrets today,
# but keep it out of git anyway — it's a machine-local settings file)
tools/dictate/.venv/
tools/dictate/config.json
